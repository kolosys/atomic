# snowflake API

Complete API documentation for the snowflake package.

**Import Path:** `github.com/kolosys/atomic/snowflake`

## Package Documentation



## Constants

**DefaultEpoch**



```go
const DefaultEpoch int64 = 1420070400000	// 2015-01-01T00:00:00Z

```

## Types

### Nullable
Nullable represents a snowflake that can be null in JSON.

#### Example Usage

```go
// Create a new Nullable
nullable := Nullable{
    ID: Snowflake{},
    Valid: true,
}
```

#### Type Definition

```go
type Nullable struct {
    ID Snowflake
    Valid bool
}
```

### Fields

| Field | Type | Description |
| ----- | ---- | ----------- |
| ID | `Snowflake` |  |
| Valid | `bool` |  |

## Methods

### MarshalJSON



```go
func (Slice) MarshalJSON() ([]byte, error)
```

**Parameters:**
  None

**Returns:**
- []byte
- error

### UnmarshalJSON



```go
func (*Slice) UnmarshalJSON(data []byte) error
```

**Parameters:**
- `data` ([]byte)

**Returns:**
- error

### Slice
Slice represents a slice of snowflakes with JSON marshaling support.

#### Example Usage

```go
// Example usage of Slice
var value Slice
// Initialize with appropriate value
```

#### Type Definition

```go
type Slice []Snowflake
```

## Methods

### MarshalJSON



```go
func (Slice) MarshalJSON() ([]byte, error)
```

**Parameters:**
  None

**Returns:**
- []byte
- error

### UnmarshalJSON



```go
func (*Slice) UnmarshalJSON(data []byte) error
```

**Parameters:**
- `data` ([]byte)

**Returns:**
- error

### Snowflake
Snowflake represents a unique ID generated by a snowflake algorithm.

#### Example Usage

```go
// Create a new Snowflake
snowflake := Snowflake{

}
```

#### Type Definition

```go
type Snowflake struct {
}
```

### Constructor Functions

### New

New creates a new Snowflake with the given value and epoch.

```go
func New(value int64, epoch int64) Snowflake
```

**Parameters:**
- `value` (int64)
- `epoch` (int64)

**Returns:**
- Snowflake

### NewFromInt64

NewFromInt64 creates a new Snowflake from an int64.

```go
func NewFromInt64(value int64, epoch int64) Snowflake
```

**Parameters:**
- `value` (int64)
- `epoch` (int64)

**Returns:**
- Snowflake

### NewFromString

NewFromString creates a new Snowflake from a string.

```go
func NewFromString(s string, epoch int64) Snowflake
```

**Parameters:**
- `s` (string)
- `epoch` (int64)

**Returns:**
- Snowflake

### NewRandom

NewRandom creates a new random Snowflake.

```go
func NewRandom(epoch int64) Snowflake
```

**Parameters:**
- `epoch` (int64)

**Returns:**
- Snowflake

## Methods

### Epoch

Epoch returns the epoch used by this Snowflake.

```go
func (Snowflake) Epoch() int64
```

**Parameters:**
  None

**Returns:**
- int64

### Int64

Int64 returns the int64 value of the Snowflake.

```go
func (Snowflake) Int64() int64
```

**Parameters:**
  None

**Returns:**
- int64

### IsValid

IsValid checks if the Snowflake has a valid non-zero value.

```go
func (Snowflake) IsValid() bool
```

**Parameters:**
  None

**Returns:**
- bool

### IsZero

IsZero returns true if the Snowflake is zero/unset.

```go
func (Snowflake) IsZero() bool
```

**Parameters:**
  None

**Returns:**
- bool

### MarshalJSON

MarshalJSON implements json.Marshaler.

```go
func (Slice) MarshalJSON() ([]byte, error)
```

**Parameters:**
  None

**Returns:**
- []byte
- error

### String

String returns the string representation of the Snowflake.

```go
func (Snowflake) String() string
```

**Parameters:**
  None

**Returns:**
- string

### Time

Time extracts the timestamp from the Snowflake.

```go
func (Snowflake) Time() time.Time
```

**Parameters:**
  None

**Returns:**
- time.Time

### UnmarshalJSON

UnmarshalJSON implements json.Unmarshaler.

```go
func (*Slice) UnmarshalJSON(data []byte) error
```

**Parameters:**
- `data` ([]byte)

**Returns:**
- error

## External Links

- [Package Overview](../packages/snowflake.md)
- [pkg.go.dev Documentation](https://pkg.go.dev/github.com/kolosys/atomic/snowflake)
- [Source Code](https://github.com/kolosys/atomic/tree/main/snowflake)
